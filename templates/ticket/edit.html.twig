{% extends 'base.html.twig' %}

{% block title %}Edit Ticket{% endblock %}

{% block body %}
<div class="container">
    <div class="jumbotron">
        <div class="text-center user-title">user: <strong class="user-title-bigger">{{ app.user.email }}</strong></div>
        <div class="row">
            <div class="col">
                <div class="card bg-light">
                <h1 class="ml-2">Edit Ticket</h1>
                {{ include('ticket/_form.html.twig', {'button_label': 'Update'}) }}
                {{ include('ticket/_delete_form.html.twig') }}
                </div>
                <a href="{{ path('ticket_index') }}">back to list</a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
