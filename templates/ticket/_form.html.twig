{{ form_start(form, { 'attr': {'class': 'm-2'}}) }}
    {{ form_widget(form, {'attr': {'class': 'btn-edit'}}) }}

    <button class="btn btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}