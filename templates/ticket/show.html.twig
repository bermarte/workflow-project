{% extends 'base.html.twig' %}

{% block title %}Ticket{% endblock %}

{% block body %}
<div class="container">
    <div class="jumbotron">
        <div class="text-center user-title">user: <strong class="user-title-bigger">{{ app.user.email }}</strong></div>
        <div class="row">
            <div class="col">
                <div class="card bg-light">
                <h1>Ticket</h1>

                <table class="table">
                    <tbody>
                        <tr>
                            <th>Id</th>
                            <td>{{ ticket.id }}</td>
                        </tr>
                        <tr>
                            <th>Text_body</th>
                            <td>{{ ticket.textBody }}</td>
                        </tr>
                        <tr>
                            <th>Is_public</th>
                            <td>{{ ticket.isPublic ? 'Yes' : 'No' }}</td>
                        </tr>
                        <tr>
                            <th>Is_open</th>
                            <td>{{ ticket.isOpen ? 'Yes' : 'No' }}</td>
                        </tr>
                        <tr>
                            <th>Is_in_progress</th>
                            <td>{{ ticket.isInProgress ? 'Yes' : 'No' }}</td>
                        </tr>
                        <tr>
                            <th>Customer_comment</th>
                            <td>{{ ticket.customerComment }}</td>
                        </tr>
                        <tr>
                            <th>Agent_comment</th>
                            <td>{{ ticket.agentComment }}</td>
                        </tr>
                        <tr>
                            <th>Is_waiting_for_feedback</th>
                            <td>{{ ticket.isWaitingForFeedback ? 'Yes' : 'No' }}</td>
                        </tr>
                        <tr>
                            <th>Is_escalated</th>
                            <td>{{ ticket.isEscalated ? 'Yes' : 'No' }}</td>
                        </tr>
                        <tr>
                            <th>User_id</th>
                            <td>{{ ticket.userId }}</td>
                        </tr>
                    </tbody>
                </table>

                <a href="{{ path('ticket_index') }}">back to list</a>

                <a href="{{ path('ticket_edit', {'id': ticket.id}) }}">edit</a>
                </div>
            </div>
        </div>
    </div>
</div>

    {{ include('ticket/_delete_form.html.twig') }}
{% endblock %}
